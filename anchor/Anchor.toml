[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[programs.devnet]
my_project = "YourProgramIDHere"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
